import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';


ReactDOM.render( <App />, document.getElementById('root') );
// App se pone como una etiqueta HTML y es el componente que voy a renderizar (pintar).
// En donde lo voy a renderizar? En el elemento con el Id 'root'. 


